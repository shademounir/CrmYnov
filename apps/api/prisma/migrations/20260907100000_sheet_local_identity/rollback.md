# Application rollback

Disable local-row connectors before reverting to an application version that does not support them. Preserve both tables, configurations, receipts, rows and audits. No backfill or destructive rollback is authorized. Existing external-ID connectors are unchanged.
