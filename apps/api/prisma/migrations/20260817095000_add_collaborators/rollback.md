# Rollback applicatif

Revenir à la version précédente. La table additive `collaborators` reste inutilisée et ses données synthétiques de CI disparaissent avec la base éphémère. Toute suppression future relève de `manual-po`.
