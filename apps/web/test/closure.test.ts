import assert from"node:assert/strict";import test from"node:test";import ClosurePage from"../app/leads/[leadId]/closure/page.js";test("renders the synthetic separated closure approval",()=>{const value=JSON.stringify(ClosurePage());for(const expected of["Inscrit","Sans suite","Preuve métier non documentaire","Soumettre au Manager","statut reste inchangé"])assert.match(value,new RegExp(expected));});
