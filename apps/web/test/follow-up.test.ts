import assert from "node:assert/strict"; import test from "node:test"; import FollowUpsPage from "../app/leads/[leadId]/follow-ups/page.js"; test("renders the controlled persistent follow-up journey",()=>{ const value=JSON.stringify(FollowUpsPage()); for(const expected of ["Relances du lead","notification interne unique","trace d.audit"]) assert.match(value,new RegExp(expected)); });
