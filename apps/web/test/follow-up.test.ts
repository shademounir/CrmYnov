import assert from "node:assert/strict"; import test from "node:test"; import FollowUpsPage from "../app/leads/[leadId]/follow-ups/page.js"; test("renders the controlled synthetic follow-up journey",()=>{ const value=JSON.stringify(FollowUpsPage()); for(const expected of ["Programmer une relance","Date et heure UTC","Reporter","Marquer traitée","notification interne unique"]) assert.match(value,new RegExp(expected)); });
