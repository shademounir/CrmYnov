import assert from"node:assert/strict";import test from"node:test";import CollaboratorsPage from"../app/leads/[leadId]/collaborators/page.js";test("renders controlled secondary collaborators without financial rules",()=>{const value=JSON.stringify(CollaboratorsPage());for(const expected of["Affectataire principal","Contributeur inscription","aucune prime calculée","Validation Manager","Aucun accès supplémentaire"])assert.match(value,new RegExp(expected));});
