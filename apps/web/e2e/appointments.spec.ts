import { expect, test } from "@playwright/test";
test("synthetic appointment agenda remains accessible and code-only",async({page})=>{ await page.route("**/api/crm/appointments?*", (route) => route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify({ items: [{ id: "appointment-synthetic", startsAt: "2026-08-27T10:00:00Z", type: "VISITE_CAMPUS", mode: "SUR_SITE", state: "PLANIFIE", campus: "SYNTHETIC" }] }) })); await page.goto("/appointments?view=table&campus=Synthetic"); await expect(page.getByRole("heading",{name:"Agenda des rendez-vous"})).toBeVisible(); await expect(page.getByRole("table")).toBeVisible(); await expect(page.getByText(/Calendriers externes.*désactivés/)).toBeVisible(); await page.getByRole("link",{name:"Jour"}).focus(); await expect(page.getByRole("link",{name:"Jour"})).toBeFocused(); });
