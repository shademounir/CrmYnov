import React from "react";

const states = ["MANQUANT", "ATTENDU", "REÇU", "À_VÉRIFIER", "VALIDÉ", "REFUSÉ", "EXPIRÉ", "REMPLACÉ"];
export default async function CandidateDocumentsPage({ params }: Readonly<{ params: Promise<{ leadId: string }> }>): Promise<React.JSX.Element> {
  const { leadId } = await params;
  return <main><nav aria-label="Fil d’Ariane"><a href={`/leads/${leadId}`}>Dossier du lead</a> / Documents</nav><h1>Checklist du dossier candidat</h1><p>Référence opaque : <code>{leadId}</code>. Le contenu des fichiers n’est jamais affiché dans cette vue.</p><section aria-labelledby="progress"><h2 id="progress">État du dossier</h2><progress value="3" max="7">3 pièces sur 7</progress><p aria-live="polite">Dossier incomplet — pièces manquantes clairement signalées.</p></section><section><h2>Pièces contrôlées</h2><table><caption>Statuts documentaires synthétiques</caption><thead><tr><th>Type</th><th>État</th><th>Action</th></tr></thead><tbody><tr><td>Baccalauréat</td><td><strong>À vérifier</strong></td><td><button type="button">Consulter les métadonnées</button></td></tr><tr><td>Relevé de notes</td><td>Manquant</td><td><button type="button">Déposer une pièce synthétique</button></td></tr></tbody></table></section><details><summary>Transitions autorisées</summary><ul>{states.map((state) => <li key={state}>{state}</li>)}</ul></details><aside aria-label="Sécurité"><h2>Contrôles</h2><p>PDF, PNG et JPEG uniquement ; taille, MIME, signature et empreinte contrôlés côté serveur. Une réception ne valide ni admission, ni inscription, ni bourse.</p></aside></main>;
}
