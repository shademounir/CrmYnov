# Bootstrap Phase 0

This root exclusively owns `crmynov-bst-n7x4q2`, its billing link, and the three
minimal Phase 0 APIs. It must use a backend prefix distinct from Foundation.

This repository revision is code-only. Do not run `plan`, `apply`, `destroy`, or
`import` without a separate Product Owner authorization. The billing account is
injected outside Git. See ADR-0006 and the Phase 0 execution runbook.
